<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="LinkManager2.Dialogs.ManageListDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    CloseButtonText="Cerrar">
    <Grid Width="440" Height="500" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView Grid.Row="0"
                  x:Name="ItemsList"
                  SelectionMode="Single"
                  SelectionChanged="OnSelectionChanged"
                  AutomationProperties.Name="Elementos existentes">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="RenameBox"
                     Grid.Column="0"
                     IsEnabled="False"
                     PlaceholderText="Nuevo nombre…"
                     AutomationProperties.Name="Renombrar seleccionado a" />
            <Button x:Name="RenameButton"
                    Grid.Column="1"
                    IsEnabled="False"
                    Content="Guardar"
                    Click="OnRenameClick"
                    AutomationProperties.Name="Guardar nuevo nombre" />
            <Button x:Name="DeleteButton"
                    Grid.Column="2"
                    IsEnabled="False"
                    Content="Borrar"
                    AutomationProperties.Name="Borrar seleccionado">
                <Button.Flyout>
                    <Flyout x:Name="DeleteFlyout">
                        <StackPanel Spacing="8" MaxWidth="260">
                            <TextBlock x:Name="DeleteConfirmText"
                                       TextWrapping="Wrap" />
                            <Button Content="Confirmar borrado"
                                    Style="{StaticResource AccentButtonStyle}"
                                    Click="OnDeleteConfirmClick"
                                    HorizontalAlignment="Stretch" />
                        </StackPanel>
                    </Flyout>
                </Button.Flyout>
            </Button>
        </Grid>

        <Grid Grid.Row="2" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="NewNameBox"
                     Grid.Column="0"
                     PlaceholderText="Añadir nuevo elemento…"
                     AutomationProperties.Name="Nombre del nuevo elemento" />
            <Button x:Name="AddButton"
                    Grid.Column="1"
                    Content="Añadir"
                    Click="OnAddClick"
                    AutomationProperties.Name="Añadir" />
        </Grid>

        <InfoBar Grid.Row="3"
                 x:Name="StatusBar"
                 IsOpen="False"
                 IsClosable="False"
                 AutomationProperties.LiveSetting="Polite" />
    </Grid>
</ContentDialog>
